from poisson_approval import EventTrio1t


def test():
    """
        >>> from fractions import Fraction
        >>> EventTrio1t(candidate_x='a', candidate_y='b', candidate_z='c',
        ...             tau_a=Fraction(1, 10), tau_ab=Fraction(6, 10), tau_c=Fraction(3, 10))
        <asymptotic = exp(- inf)>
    """
    pass
