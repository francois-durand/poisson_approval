from poisson_approval import EventPivotTjk


def test():
    """
        >>> from fractions import Fraction
        >>> EventPivotTjk(candidate_x='a', candidate_y='b', candidate_z='c', symbolic=True,
        ...               tau_a=Fraction(1, 10), tau_ab=Fraction(6, 10), tau_c=Fraction(3, 10))
        <asymptotic = exp(- n/10 + log(n) - log(10) + o(1)), phi_a = 0, phi_c = 1, phi_ab = 1>
    """
    pass
